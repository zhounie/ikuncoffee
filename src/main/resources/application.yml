spring:
  application:
    name: ikuncoffee

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://121.199.13.81:3306/fishing_wild_place
    username: root
    password: Zn19981130.

  sql:
    init:
      mode: always
      platform: mysql

logging:
  level:
    root: debug
  pattern:
    console: "%d - %m%n"


server:
  port: 8888

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
  show-actuator: true
